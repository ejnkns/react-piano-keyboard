{
  "name": "react-piano-keyboard",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/example",
    "build": "npm run build -w packages/shared && npm run build -w packages/piano-keyboard && npm run build -w packages/controls && npm run build -w packages/react-piano-keyboard && npm run build -w apps/example",
    "typecheck": "npm run typecheck -w packages/react-piano-keyboard && npm run typecheck -w apps/example",
    "test": "npm run test -w packages/shared"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.3.1",
    "concurrently": "^9.2.3",
    "postcss": "^8.5.15",
    "prettier": "3.8.4",
    "tailwindcss": "^4.3.1"
  }
}
